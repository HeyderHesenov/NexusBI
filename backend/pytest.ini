[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    eval_llm: NL->SQL eval against the real model — opt-in (NEXUSBI_EVAL_LLM=1), costs money, skipped otherwise
filterwarnings =
    ignore::DeprecationWarning
