# Test-only dependencies. Kept out of requirements.txt so the production image
# ships no test runner — pytest and an SMTP server are attack surface a serving
# container has no use for.
#
# Install alongside the runtime set:  pip install -r requirements.txt -r requirements-dev.txt
pytest==9.0.3
pytest-asyncio==1.3.0
aiosmtpd==1.4.6  # in-process SMTP sink for the live report-delivery test
